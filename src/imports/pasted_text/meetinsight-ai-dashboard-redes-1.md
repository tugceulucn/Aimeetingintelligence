Redesign the existing dashboard of an AI meeting intelligence product called “MeetInsight AI” into a futuristic, high-end, visually striking experience.

IMPORTANT:
The current dashboard is clean but too basic. Transform it into a premium AI control center with strong visual identity, depth, and motion.

---

STYLE:

* Dark mode first (no white backgrounds)
* Deep base color: #0B0B0F
* Signature gradient system:
  Purple (#6D28D9)
  Pink (#EC4899)
  Cyan (#22D3EE)
* Use layered gradients, glow effects, and lighting
* Glassmorphism cards with blur (20px)
* Soft borders with subtle transparency
* Premium SaaS aesthetic (Linear + Vercel + Apple)

---

LAYOUT IMPROVEMENTS:

* Increase spacing and hierarchy
* Use a 12-column grid system
* Add more visual separation between sections
* Introduce depth using layers and shadows

---

SIDEBAR (LEFT NAVIGATION):

* Dark translucent background with blur
* Active item:
  glowing gradient pill (purple → pink)
* Hover:
  soft glow + slight slide right
* Icons:
  minimal + animated on hover (scale 1.1)
* Add subtle vertical gradient lighting behind sidebar

---

TOP BAR:

* Transparent with blur effect
* Search input:
  glowing border on focus
* “Upload Recording” button:
  gradient background + glow + hover pulse
* Notification icon:
  pulsing dot animation

---

DASHBOARD CARDS (STATS):

Redesign all metric cards:

* Add gradient glow behind numbers
* Add micro animations:
  numbers count up on load
* Add subtle gradient overlay
* Hover:
  lift + glow expansion

Example:
“Meeting Productivity Score”
→ glowing number + animated trend arrow

---

CHARTS (VERY IMPORTANT):

Make charts feel alive:

* Bars/lines:
  gradient fill (purple → pink → cyan)
* Add glow under lines
* Add hover animation:
  highlight data point + tooltip glow
* Animate on load:
  lines draw from left
  bars rise smoothly

---

MEETING PRODUCTIVITY TREND:

* Add soft neon glow to line
* Animate line drawing
* Add gradient area fill under chart

---

MEETING TIME DISTRIBUTION:

* Bars:
  gradient + glow
* Hover:
  bar expands slightly + glow intensifies

---

AI INSIGHTS / RECOMMENDATIONS PANEL:

This is key differentiator:

* Add “AI pulse” animation (soft breathing glow)
* Add icon animation (brain or spark)
* Each recommendation:
  hover highlight + left border glow
* Add subtle shimmer effect on loading

---

MEETINGS TABLE:

Transform table into premium UI:

* Row hover:
  highlight + glow
* Add soft separators instead of hard lines
* Score badges:
  gradient color based on value
* Add micro-interaction:
  row expands slightly on hover

---

ACTION ITEMS:

* Add status colors with glow:
  Pending → orange glow
  In progress → purple glow
  Completed → green glow
* Add checkbox animation (smooth tick)

---

DECISIONS BOARD:

* Cards:
  glass cards with colored gradient borders
* Hover:
  lift + glow
* Add subtle drag-and-drop animation

---

TEAM ANALYTICS:

* Charts:
  same glowing gradient system
* Add animated progress bars
* Add micro interactions on hover

---

BACKGROUND:

* Add animated “plasma gradient”:
  slow-moving radial gradients
* Very subtle, not distracting
* Adds depth and premium feel

---

ANIMATION SYSTEM:

* All elements fade + slide in on load
* Stagger animations (delay between cards)
* Hover = lift + glow
* Buttons = glow + ripple effect
* Charts animate on render
* Background moves slowly (15s loop)

---

UX DETAILS:

* Everything should feel responsive and alive
* No static elements
* Smooth transitions everywhere
* Maintain readability and clarity

---

OUTPUT:

* High-fidelity dashboard UI
* Premium AI SaaS feel
* Visually unique and differentiated from competitors
* Strong use of gradients, glow, and motion
